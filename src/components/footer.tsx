import * as React from "react";
import { Component } from "react";

interface FooterProps {}

const Footer: React.FunctionComponent<{}> = (props: FooterProps) => {
  return <div>Footer</div>;
};

export default Footer;
